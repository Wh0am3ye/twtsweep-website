diff --git a/components.js b/components.js
index 2829641..a38b1eb 100644
--- a/components.js
+++ b/components.js
@@ -35,6 +35,9 @@ function setActiveNavLink() {
     path += "index.html";
   }
   path = path.replace(/\/+$/, "");
+  if (!path.endsWith(".html")) {
+    path += ".html";
+  }
 
   document.querySelectorAll(".menu a").forEach(link => {
     let linkPath = link.pathname;
@@ -42,6 +45,9 @@ function setActiveNavLink() {
       linkPath += "index.html";
     }
     linkPath = linkPath.replace(/\/+$/, "");
+    if (!linkPath.endsWith(".html")) {
+      linkPath += ".html";
+    }
 
     if (linkPath === path) {
       link.setAttribute("aria-current", "page");
@@ -65,6 +71,13 @@ function updateLangButton() {
   // Remove relative weirdness (optional but safer)
   path = path.replace(/\/+$/, "");
 
+  // Cloudflare Pages serves/redirects clean URLs without the .html
+  // extension in production, so window.location.pathname won't match
+  // the .html-keyed langMap unless we add it back here.
+  if (!path.endsWith(".html")) {
+    path += ".html";
+  }
+
   // Set correct link
   if (langMap[path]) {
     langBtn.href = langMap[path];
